
{% extends 'base.html.twig' %}

{% block title 'Materiel d occasion : ' %}

{% block body %}
    <h1>changez votre password</h1>
	
     {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'action.save'|trans }}
        </button>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
		
    <div class="section">
        <a href="{{ path('admin.user.edit') }}" class="btn btn-warning">
            <i class="fa fa-edit" aria-hidden="true"></i> {{ 'admin.edit.user'|trans }}
        </a>
    </div>

    
  
{% endblock %}
