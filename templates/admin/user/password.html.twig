
{% extends 'base.html.twig' %}

{% block title 'Materiel d occasion : ' %}

{% block body %}
    <h1>hello changez votre password : </h1>
	
     {{ form_start(form) }}
	 
	 <div class="row">
	 <div class="col-md-4">{{ form_row(form.username) }}</div>
    <div class="col-md-4">{{ form_row(form.plainPassword) }}</div>
		</div>
	<div class="row">
    <div class="col-md-4">{{ form_row(form.password, { 'attr' : { 'value': 'nouveau' } }) }}</div>
   
	</div>
       
        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'save' }}
        </button>
    {{ form_end(form) }}
{% endblock %}

{% block sidebar %}
		
    <div class="section">
        <a href="{{ path('admin.user.edit') }}" class="btn btn-warning">
            <i class="fa fa-edit" aria-hidden="true"></i> {{ 'admin.edit.user' }}
        </a>
    </div>

    
  
{% endblock %}
